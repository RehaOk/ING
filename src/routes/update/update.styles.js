/* These classes are essentially the same as create page styles but
since it is a demo and there isn't that much redundancy i kept them */

import {css} from 'lit';

export const updateViewStyles = css`
  .update-view {
    padding: 0 2rem;
    margin-bottom: 1rem;
  }

  .update-view__title {
    display: inline-block;
    color: var(--primary-color);
    font-weight: 500;
    font-size: 1.3rem;
    margin-left: 15px;
  }
`;
