import {css} from 'lit';

export const createViewStyles = css`
  .create-view {
    padding: 0 2rem;
    margin-bottom: 1rem;
  }

  .create-view__title {
    display: inline-block;
    color: var(--primary-color);
    font-weight: 500;
    font-size: 1.3rem;
    margin-left: 0.9rem;
  }
`;
